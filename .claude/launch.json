{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "dev", "--", "--host", "0.0.0.0"],
      "autoPort": true,
      "webRoot": "${workspaceFolder}"
    }
  ]
}
